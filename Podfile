# Uncomment the next line to define a global platform for your project
platform :ios, '12.0'

target 'AnylineUIKit' do
use_frameworks!
  pod 'Anyline', '>= 25'
  pod 'SnapKit', '~> 5.0'
  pod 'OpenCV'

end
